# (Windows Defender may prevent you from running)
# (make sure pip and pyinstaller are installed/updated)
#
# 1. cd to directory that contains your .py file
# 2. pyinstaller ...
#   -F   (all in 1 file)
#   -w   (removes terminal window)  only if you don't need it
#   -i icon.ico  (adds custom icon to .exe) needs to be an .ico file (online conversion)
#   Clock" "program.py  (name of your main python file), if there is a space in the name -> 1stpart" "secondpart, basically space = " "
#
# 3. exe is located in the dist folder, we can drag the .exe file to wherever we want, then we can delete all the other things that we made



